Task: Microservice for converting Roman Numerals to Arabic Numbers
API specification:
    - Method GET
    - Route root such as localhost:3000/
    - Input Query String Parameter called “roman”
    - Output Plain String (output is calculated from the input. The input is Roman Numeral which is converted to Arabic Number)
Requirements:
    - Running in docker
    - Preferred NodeJS or Java/Scala 
    - Recommended time spent to finish the task: couple of hours
    - Submission by email with link to repository or zip file

==============================

- tests
- typescript
- 401 if input is not roman

// ls -l
// Dockerfile
// package.json
// package-lock.json
// src

==============================
- docker file
FROM node:11.11
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install
COPY src ./src
RUN npm build && npm test
CMD node ./src/app.js

// docker build . --tag foo
// docker run -it -p 8080:8080 foo
